/** Preset instructions users paste into ChatGPT / Claude / 豆包 / 通义 / Gemini with WeChat screenshots. */
export const AI_IMPORT_PROMPT = `请对下方/附件里的微信聊天长截图（可能有多张）做直接 OCR 识别，把屏幕上能读到的对话原样转成纯文本聊天记录。

核心原则：你是 OCR，不是总结助手。只识别、只转录，不理解发挥、不补全、不改写。

严格要求：
1. 只输出聊天正文，不要任何说明、标题、评论、前言后语、识别置信度，也不要使用 markdown 代码块（不要用 \`\`\` 包裹）。
2. 每行一条消息，格式优先「姓名：内容」。例如：
   我：在吗？
   小美：哈哈你还记得啊
   若截图带时间，也可用：
   我 2024-01-01 12:34
   消息内容
   或：[12:34] 我：消息内容
3. 【OCR】逐气泡、按截图从上到下（多图按上传顺序）识别文字；看不清的字用 □ 占位，禁止猜测编造。
4. 【说话人 / 我】微信截图里自己一侧通常不显示名字（气泡在右侧）。因此：
   - 自己发的消息，说话人默认写成「我」，不要留空、不要写成「未知」「用户」。
   - 只有截图里能明确认出自己的昵称/备注时，才用那个名字；一旦选定就全文统一，不要中途改名。
   - 对方用截图里出现的昵称；对方名字前后必须保持一致。
   - 不要合并、臆造截图中未出现的人。
5. 原样保留错别字、口语、标点、表情文字；不要润色、翻译、删改、合并或总结原话。
6. 跳过系统提示、撤回提示、时间分割线、「以下为新消息」等非对话噪音；图片/语音/表情等媒体若无文字可读，用 [图片]、[语音]、[动画表情] 等简短占位。

现在开始 OCR，直接输出纯文本聊天记录：`;
